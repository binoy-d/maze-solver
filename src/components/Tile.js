import React from 'react';
import './Tile.css';

function Tile({size}){
    return (
        <div style = {{width:size, height:size}} className = "tile-wrapper">
            
        </div>

    );
}

export default Tile;